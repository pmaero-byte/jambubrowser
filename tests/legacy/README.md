"""Legacy phase tests (Phases 2–5).

These were written during early feature development and are NOT part of the
current green test suite. They are quarantined here so `make test` / CI report
honest numbers instead of bundling bit-rotted assertions.

Run them explicitly if you want to revive coverage for those modules:

    pytest tests/legacy/ -v
"""
