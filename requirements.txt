# ─────────────────────────────────────────────────────────────
# Jambubrowser — Python dependencies
# ─────────────────────────────────────────────────────────────
# Install with:  pip install -r requirements.txt
# Python 3.9+ required.
# ─────────────────────────────────────────────────────────────

# Web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
starlette>=0.27.0
# Required by FastAPI for any endpoint that accepts application/x-www-form-urlencoded
# or multipart/form-data (login, vault unlock, file uploads). Without this,
# import-time fixture setup raises and 20+ tests fail with RuntimeError.
python-multipart>=0.0.6

# HTTP client
httpx>=0.25.0
requests>=2.31.0
# Optional: SOCKS5/Tor support for outbound HTTP. If JAMBU_TOR_SOCKS_URL
# is set, all engine.search / scraper / arxiv requests go through it.
httpx-socks[asyncio]>=0.7.7

# Configuration
python-dotenv>=1.0.0

# System / process
psutil>=5.9.0

# Database
sqlite-vec>=0.1.0

# Crypto (vault, audit hash chain)
cryptography>=41.0.0

# Markdown processing
markdownify>=0.11.0

# MCP server (for Claude/Cursor integration)
# Pinned <2.0: mcp 2.0 removed mcp.server.fastmcp.FastMCP (breaking upstream change).
mcp>=1.0.0,<2.0.0

# Semantic search / embeddings (optional, falls back to numpy)
sentence-transformers>=2.2.0
numpy>=1.24.0

# WebSocket support
websockets>=12.0

# Test framework
pytest>=7.4.0
pytest-asyncio>=0.21.0
anyio>=4.0.0

# Lint / format
ruff>=0.1.0
mypy>=1.7.0
