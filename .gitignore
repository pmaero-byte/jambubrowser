# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
.pytest_cache/

# Conda environment
searxng-env/

# MLX virtual environment
mlx-venv/

# Node
node_modules/
browser-app/dist/
browser-app/src-tauri/target/

# Models & binaries
models/
*.tar.gz
*.tar.bz2
*.bin
llama-b9496/

# Logs
*.log

# System
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# OpenCode
.omo/

# Database files (generated at runtime)
rag_data.db

# Docker volumes
valkey-data/

# SearXNG config (contains local settings)
searxng/
searxng-config/
searxng.log

# Staged repo backup
staged_repo/

# Playwright-MCP test artifacts (auto-generated by the Playwright MCP server
# while running browser-automation tests against the dev build). Saved as
# `.log` (console output), `.png` (screenshots), and `.yml` (accessibility
# snapshots). The `spa-landing` file at the repo root is one such accessibility
# snapshot that was committed by accident in commit bd59d68.
.playwright-mcp/
/spa-landing

# Secrets (if any)
*.key
.env
tests/.artifacts/
